li(role="presentation")
  a.g-assign-igsn-recursively(role="menuitem")
    i.icon-barcode
    | Add IGSN recursively

if showClassifyEbsd
  li(role="presentation")
    a.g-classify-ebsd(role="menuitem")
      i.icon-signal
      | Classify EBSD
